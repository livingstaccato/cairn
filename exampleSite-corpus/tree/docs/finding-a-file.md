---
title: Finding a file without guessing the path
summary: Every directory publishes index.json beside its index.html
tags: [guide]
---

Point a script at `/downloads/index.json` rather than a path you typed by
hand — it lists every entry in that directory with its size, kind and digest,
and it's the same tree the browsable page above renders from.
