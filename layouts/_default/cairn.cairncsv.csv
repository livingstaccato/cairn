{{- /* Column order must match internal/emit.CSVHeader exactly; the exampleSite
       gate asserts the two are byte-identical.

       csvField quotes a field containing a comma, quote or newline, and
       apostrophe-prefixes one opening with =, +, -, @, tab or CR — spreadsheets
       execute those as formulas, and filenames in a mirror are not trusted. */ -}}
{{- define "csvField" -}}
{{- $s := . | string -}}
{{- if and (gt (len $s) 0) (in (slice "=" "+" "-" "@") (substr $s 0 1)) }}{{ $s = printf "'%s" $s }}{{ end -}}
{{- if or (in $s ",") (in $s "\"") (in $s "\n") -}}
"{{ replace $s "\"" "\"\"" }}"
{{- else -}}
{{ $s }}
{{- end -}}
{{- end -}}
name,path,type,size,modified,sha256,kind,title
{{ range .Params.cairn.entries -}}
{{ $type := cond .is_dir "dir" "file" -}}
{{ template "csvField" .name }},{{ template "csvField" .path }},{{ $type }},{{ .size }},{{ .modified }},{{ .sha256 | default "" }},{{ .kind }},{{ template "csvField" (.title | default "") }}
{{ end -}}
